# ── SPAWN API Directory Security ────────────────────────────
# Deny directory listing
Options -Indexes

# Block direct HTTP access to files that should never be public.
# claude-proxy.php is intentionally accessible (it's the API endpoint).
<FilesMatch "^(db|init)\.(php|sql)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
