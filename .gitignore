# ── Secrets / env ──────────────────────────────
.env
.env.*
!.env.example

# ── Scratch / working drafts (kept locally, not deployed) ──
# NOTE: contains API-Key.rtf and other source material — never commit.
Artifacts/

# ── Local Claude Code settings ─────────────────
.claude/settings.local.json

# ── Node (for the upcoming Vercel/Node conversion) ──
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Vercel ─────────────────────────────────────
.vercel/

# ── OS / editor cruft ──────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
